<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Dark Mode Theme -->
    <style name="Base.Theme.ExpenseTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="android:statusBarColor">@color/statusBar_bg_dark</item>
        <item name="badgeTextColor">@color/white</item>   <!-- For Simple text color -->
        <item name="colorSecondary">@color/background_dark</item>
        <item name="colorPrimaryVariant">@color/white</item> <!--  For Credit text color -->
        <item name="colorOnPrimary">@color/white</item> <!-- for Debit Text Color -->
        <item name="textFillColor">@color/white</item>  <!-- for Title Color -->
        <item name="backgroundColor">@color/dilogBox_bg_dark</item>

        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.ExpenseTracker" parent="Base.Theme.ExpenseTracker" />

</resources>
